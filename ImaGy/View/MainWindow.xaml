<Window x:Class="ImaGy.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImaGy"
        xmlns:vm="clr-namespace:ImaGy.ViewModel"
        mc:Ignorable="d"
        Title="ImaGy" Height="900" Width="1600" MinHeight="500" MinWidth="1000">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <DockPanel>
        <!-- Menu -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_파일">
                <MenuItem Header="열기" Command="{Binding OpenImageCommand}"/>
                <MenuItem Header="저장" Command="{Binding SaveImageCommand}"/>
                <Separator />
                <MenuItem Header="종료"/>
            </MenuItem>
            <MenuItem Header="_편집">
                <MenuItem Header="되돌리기" Command="{Binding UndoCommand}"/>
                <MenuItem Header="다시 실행" Command="{Binding RedoCommand}"/>
            </MenuItem>
            <MenuItem Header="_보기">
                <MenuItem Header="작업 내역 보이기"/>
                <MenuItem Header="로그 창 보이기"/>
            </MenuItem>
            <MenuItem Header="_도움말">
                <MenuItem Header="단축키 안내"/>
                <MenuItem Header="정보"/>
            </MenuItem>
        </Menu>

        <!-- Status Bar -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="{Binding FileName}" />
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding ImageResolution}" />
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding ZoomLevel}" />
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding ProcessingTime}" />
            </StatusBarItem>
        </StatusBar>

        <!-- Main Content -->
        <Grid Background="{StaticResource WindowBackgroundBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="200"/>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" MinWidth="200"/>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="300" MinWidth="250"/>
            </Grid.ColumnDefinitions>

            <!-- Original Image View -->
            <Border Grid.Column="0" BorderThickness="1" Margin="10">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Image Source="{Binding LeftCurrentImage}" Stretch="Uniform" />
                </ScrollViewer>
            </Border>

            <GridSplitter Grid.Column="1" />

            <!-- Processed Image View -->
            <Border Grid.Column="2" BorderThickness="1" Margin="10">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Image Source="{Binding RightCurrentImage}" Stretch="Uniform" />
                </ScrollViewer>
            </Border>

            <GridSplitter Grid.Column="3" />

            <!-- Right Side Panel -->
            <Grid Grid.Column="4" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="200" />
                </Grid.RowDefinitions>

                <!-- Button Groups -->
                <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <GroupBox Header="기본 기능">
                            <StackPanel>
                                <Button Content="확대/축소" Margin="2"/>
                                <Button Content="자르기" Margin="2"/>
                            </StackPanel>
                        </GroupBox>
                        <GroupBox Header="기본 영상 처리">
                            <StackPanel>
                                <Button Content="이진화" Command="{Binding BinarizeCommand}" Margin="2"/>
                                <Button Content="평활화" Margin="2"/>
                            </StackPanel>
                        </GroupBox>
                        <GroupBox Header="필터링">
                            <StackPanel>
                                <Button Content="샤프닝" Margin="2"/>
                                <Button Content="블러" Margin="2"/>
                                <Button Content="엠보싱" Margin="2"/>
                            </StackPanel>
                        </GroupBox>
                        <GroupBox Header="형태학적 처리">
                            <StackPanel>
                                <Button Content="팽창" Margin="2"/>
                                <Button Content="침식" Margin="2"/>
                            </StackPanel>
                        </GroupBox>
                         <GroupBox Header="템플릿 매칭">
                            <StackPanel>
                                <Button Content="NCC" Margin="2"/>
                                <Button Content="SAD" Margin="2"/>
                                <Button Content="SSD" Margin="2"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
                
                <!-- History -->
                <GroupBox Grid.Row="1" Header="작업 내역">
                    <ListBox x:Name="작업내역" ItemsSource="{Binding HistoryItems}" d:ItemsSource="{d:SampleData ItemCount=5}" />
                </GroupBox>

                <!-- Log -->
                <GroupBox Grid.Row="2" Header="로그">
                    <TextBox Text="{Binding LogText}" IsReadOnly="True" VerticalScrollBarVisibility="Auto" />
                </GroupBox>
            </Grid>
        </Grid>
    </DockPanel>
</Window>