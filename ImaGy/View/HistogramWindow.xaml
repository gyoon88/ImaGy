<Window x:Class="ImaGy.View.HistogramWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImaGy.View"
        xmlns:vm="clr-namespace:ImaGy.ViewModels"
        mc:Ignorable="d"
        Title="Histogram" Height="400" Width="600"
        d:DataContext="{d:DesignInstance Type=vm:HistogramViewModel, IsDesignTimeCreatable=True}">

    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Name="YAxisLabelsPanel" VerticalAlignment="Stretch" Margin="5,10,5,10"/>

        <Canvas Grid.Row="0" Grid.Column="1" Name="HistogramCanvas" Background="{StaticResource PrimaryGrayBrush}" Margin="10" SizeChanged="HistogramCanvas_SizeChanged"/>

        <StackPanel Grid.Row="1" Grid.Column="1" Name="XAxisLabelsPanel" Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="10,5,10,0"/>

    </Grid>
</Window>