<Window x:Class="ImaGy.View.HistogramWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImaGy.View"
        mc:Ignorable="d"
        Title="Histogram" Height="400" Width="600">
    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Canvas Grid.Row="0" Name="HistogramCanvas" Background="{StaticResource PrimaryGrayBrush}" Margin="10" Loaded="HistogramCanvas_Loaded" SizeChanged="HistogramCanvas_SizeChanged"/>
        <Button Grid.Row="1" Content="Update Histogram" x:Name="HitogramUpdate" Margin="100,0,100,10" HorizontalContentAlignment="Center" /> 
    </Grid>
</Window>